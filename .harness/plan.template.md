# 작업 계획

> Claude Code가 초안을 만들고 사람이 승인합니다. Codex는 이 파일의 태스크 단위로만 작업합니다.

## 목표

(한 문장. 이 작업이 끝나면 무엇이 가능해지는가)

## 공통 제약

- 건드리면 안 되는 경로:
- 추가 금지 의존성:

---

## TASK-001: (태스크 이름)

- **depends_on**: none
- **수정 대상 파일**:
  - `path/to/file.py` (수정)
  - `path/to/new_file.py` (신규)
- **인터페이스 확정**:
  ```
  def parse_config(path: str) -> Config
  ```
- **구현 내용**:
  1. (구체적으로. "적절히 처리" 같은 표현 금지)
  2.
- **검증 명령**:
  ```
  pytest tests/test_config.py -q
  ```
- **완료 조건**: 위 명령이 종료 코드 0으로 끝난다
- **금지 사항**:
  - 기존 함수 시그니처 변경 금지

---

## TASK-002: (태스크 이름)

- **depends_on**: TASK-001
- (이하 동일 구조)
