아래 계획 파일의 지정된 태스크 하나만 구현한다.

절대 규칙:
- 지정된 태스크 외의 다른 태스크는 건드리지 않는다.
- "수정 대상 파일"에 없는 파일은 생성하거나 수정하지 않는다.
- "인터페이스 확정" 섹션의 시그니처를 정확히 그대로 사용한다.
- "금지 사항"을 위반하지 않는다.
- 구현 후 "검증 명령"을 실행하고, 실패하면 스스로 수정해 최대 2회까지 재시도한다.
- 마지막에 실행한 검증 명령의 출력을 그대로 남긴다.

작업할 태스크 ID: {{TASK_ID}}

계획 파일 내용:
{{PLAN}}
