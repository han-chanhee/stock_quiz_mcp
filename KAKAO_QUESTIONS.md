# 카카오 디스코드 문의용 질문 목록

이미 등록된 MCP 서버(stock-quiz-mcp-kakaotools, mcpId: 3556)에 환경변수를
추가하는 방법을 콘솔에서 찾지 못해 확인이 필요합니다.

---

## 상황 설명 (먼저 이대로 전달)

```
stock-quiz-mcp-kakaotools(mcpId: 3556) 서버에 OAuth 활성화용 환경변수
(OAUTH_ENABLED=1, OAUTH_MCP_ID=3556)를 추가하려고 하는데, 방법을 못 찾았습니다.

- 서버 상세 페이지에는 "추가 옵션 > 환경변수: 등록된 환경변수가 없습니다"라고만
  표시되고, 수정/추가 버튼이 안 보입니다.
- "재배포" 버튼을 눌러도 확인창만 뜨거나 바로 Redeploying 상태로 바뀔 뿐,
  환경변수를 입력하는 화면은 안 나옵니다.
```

## 질문 1
이미 등록된 서버에 환경변수를 나중에 추가/수정할 수 있는 화면이 있나요?
있다면 정확한 메뉴 경로를 알려주세요.

## 질문 2
환경변수 추가가 "재등록"(서버를 새로 만드는 것)을 통해서만 가능하다면,
재등록 시 Endpoint URL이 바뀌나요? (바뀌면 안 됩니다 — 이미 디스코드로
제출한 URL이라 유지해야 합니다)

## 질문 3
"새로운 MCP 서버 등록" 폼으로 기존 서버와 같은 MCP 식별자·Endpoint를
다시 입력해서 저장하면, 기존 서버가 갱신되나요 아니면 별개의 새 서버가
생성되나요?

## 질문 4
환경변수를 아예 콘솔에서 지정할 수 없는 구조라면(이미지에 빌드타임으로
고정해야 하는 방식이라면), 그 방식을 안내해주실 수 있나요?

---

## 답변 받으면 이 파일에 기록

- 질문 1 답:
- 질문 2 답:
- 질문 3 답:
- 질문 4 답:
