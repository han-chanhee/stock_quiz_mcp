# 8/7(금) 할 일 — 본선용 서버 생성 + 엔드포인트 제출

오늘 목표는 **엔드포인트 확보와 디스코드 제출**뿐이다. 완성도 심사 아님.
데이터 갱신·위젯·툴 추가는 **8/23까지** 하면 된다. 오늘 손대지 말 것.

⚠️ **예선 서버(`stock-quiz-mcp`)는 절대 삭제·중지 금지.** 그대로 둔다.

---

## 1. KC 등록 폼 입력값 (그대로 복붙)

| 항목 | 값 |
|---|---|
| MCP 서버 이름 | `stock-quiz-mcp-kakaotools` |
| 설명 | 아래 문단 복사 |
| Registry 호스트 | `ghcr.io` |
| Registry 사용자 / 비번 | **비워둠** (public이라 익명 pull 됨 — 확인함) |
| image_name | `han-chanhee/stock-quiz-mcp` |
| image_tag | `latest` |
| 환경변수 | **추가 안 함** (이미지에 이미 구워져 있음) |
| 시크릿 | **추가 안 함** (서버 기동에 API 키 불필요) |
| 컨테이너 포트 | **`8080`** ← ⚠️ 기본값 8000을 반드시 바꿀 것 |

설명란:

```
코스피/코스닥 종목으로 즐기는 주식 퀴즈 '주식대결'입니다. 주가·시장·종목 3가지 모드로 퀴즈를 내고, 오답이면 힌트, 정답이면 현재가·시총순위·팩트 기반 미니분석을 보여줍니다. 투자 권유가 아닌 정보/퀴즈 제공용입니다.
```

### 포트가 유일한 함정

이미지는 `PORT=8080`이 구워져 있고 8080을 listen한다. 폼 설명에 "containerPort·헬스체크에 사용됩니다"라고
돼 있으니 **8000으로 두면 헬스체크가 빈 포트를 찔러 배포가 죽는다.**

> 만약 8080으로 해도 Active가 안 되면 → 컨테이너 포트 `8000` + 환경변수 `PORT=8000` 추가.
> (서버가 `os.environ.get("PORT", "8000")`으로 읽으므로 둘 다 동작한다.)

### 이름을 이걸로 하는 이유
- 예선(`stock-quiz-mcp`)과 중복 불가 → 카카오가 권장한 `-kakaotools` 접미사 사용
- 디스코드 제출 시 예선↔본선 대응이 한눈에 보임
- DNS 규칙 충족(소문자·하이픈, 25자)
- **생성 후엔 이름 변경 불가**(이후 업데이트는 "재배포" 버튼) — 그래서 오늘 신중히

---

## 2. 생성 직후 검증 (2분)

`Active` 뜨면 아래 3개를 순서대로. 엔드포인트는 생성된 URL로 바꿔서.

```bash
B=https://stock-quiz-mcp-kakaotools.playmcp-endpoint.kakaocloud.io

# ① 헬스체크
curl -s $B/health
# 기대: {"status":"ok","stale":false,"data_as_of":"2026-07-08T03:52:36.791094+09:00"}

# ② MCP 핸드셰이크
curl -s -X POST $B/mcp -H "Content-Type: application/json" \
  -H "Accept: application/json, text/event-stream" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-03-26","capabilities":{},"clientInfo":{"name":"c","version":"1"}}}'
# 기대: protocolVersion 2025-03-26, serverInfo.name = stock-quiz-dictionary

# ③ 툴 2개 노출
curl -s -X POST $B/mcp -H "Content-Type: application/json" \
  -H "Accept: application/json, text/event-stream" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/list"}'
# 기대: quiz, submit_answer
```

셋 다 200이면 끝. (git bash에서 한글 인자를 직접 넣으면 cp949로 깨져 500 난다 — 위 명령은 ASCII만 써서 안전.)

---

## 3. 디스코드 제출

팀별 채널에 **서버 이름 + Endpoint URL** 전달. 오늘까지.

```
서버 이름: stock-quiz-mcp-kakaotools
Endpoint URL: https://stock-quiz-mcp-kakaotools.playmcp-endpoint.kakaocloud.io/mcp
```

※ 실제 URL은 KC 콘솔에 표시된 값을 복사해 쓸 것(위는 예상 패턴).

---

## 4. (여유 있으면) PlayMCP 임시 등록

가이드상 오늘 필수는 아니지만 해두면 Preview 테스트가 바로 된다.

- PlayMCP에 **"임시 등록"**으로 등록 — ⚠️ "등록 및 심사 요청" **아님**
- MCP 상세 미리보기 → 도구함에 추가
- Kakao Tools Preview(`preview-chatgpt.kakao.com`)에서 호출 확인 (권한 오늘까지 부여됨, ChatGPT Plus/Pro 계정 필요)

---

## 오늘 이후 (8/23까지, 오늘 하지 말 것)

1. **데이터 갱신** — `batch/data/*.json`이 7/8자다. `/health`의 `data_as_of`가 한 달 전으로 찍힌다.
2. **stale 버그** — `server/cache.py`의 stale은 *파일 부재*로만 켜지고 나이를 안 본다. 그래서 위 ①에서 `stale:false`가 나온다. 나이 기반 판정 추가 필요.
3. **Tool description 다듬기** — 본선 가이드에 *"툴 호출이 잘되도록 Tool Description을 정의하는 것도 심사 기준에 포함"*이라고 명시됨.
4. **위젯** (권장, 필수 아님) — 넣으면 LLM이 답변을 가공하지 않고 그대로 노출 + 카톡 공유용 `copy_text`.
5. 툴 개수 2개 → 가이드 권장은 3~10개.

업데이트는 전부 **"재배포" 버튼**으로. 서버 재생성 금지.
